{
	"cleanUrls": true,
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/static-build",
			"config": {
				"buildCommand": "yarn docs:build",
				"outputDirectory": ".vitepress/dist"
			}
		}
	]
}
